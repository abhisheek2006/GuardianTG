# Core
pyrogram==2.0.106
# Optional Pyrogram speedup. Requires a C compiler (no Python 3.14 wheels);
# Pyrogram works fine without it, so it is commented out to keep builds green.
# TgCrypto==1.2.5
pydantic>=2.12,<3
pydantic-settings>=2.4,<3
python-dotenv==1.0.1

# Database (MongoDB)
motor>=3.4,<4
pymongo>=4.9,<5

# Redis (state / rate limiting)
redis==5.0.4

# Background jobs
APScheduler==3.10.4

# HTTP / web dashboard
aiohttp>=3.11,<4
fastapi==0.111.0
uvicorn>=0.30,<1
jinja2>=3.1,<4
python-multipart>=0.0.9

# Testing
pytest>=8.2
pytest-asyncio>=0.23